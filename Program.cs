using System;
using System.Text.Json;
using static BankTransferConfig;

internal class program {
    public static void Main(string[] args) {
        AplikasiConfig Config = new AplikasiConfig();


        Console.WriteLine("Masukkan nominal transfer: ");
        total_biaya = nominal_transfer + biaya transfer;
        Console.WriteLine(total_biaya);

    }
}
